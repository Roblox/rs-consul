name: Main

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: "ubuntu-latest"
    container: rust:1.52.1
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: cargo build
